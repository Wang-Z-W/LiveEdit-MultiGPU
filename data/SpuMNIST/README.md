# Processed MNIST Data
This directory contains processed MNIST data with manually introduced spurious correlations for training and evaluation.
For each subdirectory, the MNIST images are stored in the 'images' directory, and the annotations are stored in the '.feather' file.